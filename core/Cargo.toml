[package]
name = "dc34-virtual-pet-core"
version = "0.1.0"
edition = "2021"
description = "Xous-independent game state and time arithmetic for the DC34 creature-raising game"

# Deliberately no dependencies: this crate must stay buildable and testable on the
# host without any of the Xous toolchain.
[dependencies]
